// Single place to update the outward-facing links.

/** App Store listing. TODO: replace the placeholder id once the app is live. */
export const APP_STORE_URL = "https://apps.apple.com/app/idXXXXXXXX";

/**
 * Android / Google Play. The app isn't on Play yet, so every "Get it on
 * Google Play" button points at the coming-soon page for now. Swap this for
 * the real `https://play.google.com/store/apps/details?id=…` URL at launch.
 */
export const PLAY_STORE_URL = "/coming-soon";

/** Inquiry / support / legal contact address. */
export const CONTACT_EMAIL = "info@gymfactor.app";

/** Canonical site origin (no trailing slash). */
export const SITE_URL = "https://www.gymfactor.app";
